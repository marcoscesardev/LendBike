<div class="field">
  <%= f.input :code %>
</div>

<% if action_name == 'edit' %>
  <%= f.input :situation, collection: Bike.translated_situations %>
<% end %>

<div class="field">
  <%= f.input :active %>
</div>
